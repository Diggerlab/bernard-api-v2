object false


node(:base_build) do 
	@base_build
end

node(:latest_build) do 
	@latest_build
end

node(:latest_version_name) do 
	@latest_version_name
end

node(:latest_version_desc) do 
	@latest_version_desc
end